#include<stdio.h>
#include<stdlib.h>

int main()
{
    int a, i=0;
    int *i2;
    while(i < 78797)
    {
        printf("welcome to shaw kailash\n");
        i2 = malloc(45443 * sizeof(int));
        if(i % 100 == 0)
        {
            getchar();
        }
        i++;
        free(i2);
    }
    return 0;
}